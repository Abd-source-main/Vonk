# https://vonkelektra.nl/robots.txt
# Vonk Installatie & Onderhoud B.V.

User-agent: *
Allow: /

# No crawlable value, and they leak form state into the index
Disallow: /*?service=
Disallow: /bedankt/
Disallow: /en/thank-you/

# Dutch is the default locale and is served straight from the site root — there
# is no language chooser and no redirect in front of it, so / is a normal
# indexable page. English lives under /en/. Both are reachable from the globe
# switcher in the nav of every page, so crawl reaches both routes from any entry.

Sitemap: https://vonkelektra.nl/sitemap.xml
